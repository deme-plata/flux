[package]
name = "flux-gui"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Flux GUI — Slint-based live UI. AI-editable via MCP tools. Hot-swap UI components."

[dependencies]
serde_json = "1"