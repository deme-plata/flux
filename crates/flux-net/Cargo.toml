[package]
name = "flux-net"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "flux-net — WireGuard mesh + Arti Tor networking primitives, extracted from fluxc-core (board P1 arch-debt split)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"
rand = "0.8"
