[package]
name = "flux-quantum-architect"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "flux-quantum-architect — workspace blueprint / 6-dimension architecture x-ray, extracted from fluxc-core (board P1 arch-debt split)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
