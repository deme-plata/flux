[package]
name = "flux-akeeba"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Flux Akeeba — Akeeba-Backup-style integrity-verified, content-addressed snapshot/restore for the flux auto-updater (backup-before-update + point-in-time rollback)"

[dependencies]
blake3.workspace = true
serde = { workspace = true }
serde_json = "1"
anyhow.workspace = true
