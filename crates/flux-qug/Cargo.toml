[package]
name = "flux-qug"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Flux-QUG Bridge — Compile Quillon Graph with fluxc. Drop-in compatible RocksDB SST files."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"
