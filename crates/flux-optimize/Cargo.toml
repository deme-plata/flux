[package]
name = "flux-optimize"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Flux optimizer — SIMD, io_uring, cache-line, perf/watt presets"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
flux-graph = { path = "../flux-graph" }
