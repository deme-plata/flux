# Vite build output — regenerated by `fluxc build`.
dist/

# Fixtures emitted by the crate's own measurement harness
# (`FLUX_WEBCAM_EMIT_DIR=… flux_webcam-<hash> tests::sap_report_for_humans`).
# Regenerable, and frame.png is ~900 KB of synthetic test pattern.
public/
