[package]
name = "flux-zk-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimum type shim layer for vendored Quillon Graph ZK crates (ValidatorId etc). Replaces q-types for the ZK closure."

[dependencies]
serde = { workspace = true }
blake3 = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
