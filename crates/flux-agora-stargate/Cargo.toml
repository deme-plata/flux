[package]
name = "flux-agora-stargate"
version.workspace = true
edition = "2021"
description = "Quillon Agora build verification × SIGIL Stargate verify-once — testnet deploy v0.2"

[dependencies]
blake3 = "1.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
fluxc-core = { path = "../fluxc-core" }
