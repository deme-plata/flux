# Rust build artifacts
/target
target/
**/target/
.target-shared/
*.rlib
*.rmeta

# Backup / editor
*.swp
*~
.DS_Store

# Local env / secrets
.env
*.key
.flux-agent-key.json

# Node (for embedded vite/gui apps)
node_modules/
dist/

# Logs
*.log

# content-addressed flux-rev stores (regenerable)
.flux-rev/
**/.flux-rev/

# generated context engine output (regenerable)
.whale/

# accidental build dir (723 MB, intent-to-add 2026-09) — never commit
target-fluxdb-check/
# scratch + build artifacts swept 2026-09-22
.flux-*.patch
.swarm_call.sh
*.bak-*
mir-corpus/*.o
docs/**/*.aux
docs/**/*.out
flux-bitunix-before-aggregator-*
aggregator-probe.json
aggregator-live-http.json
