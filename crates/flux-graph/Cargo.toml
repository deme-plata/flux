[package]
name = "flux-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Dependency graph resolution — Phase 2 cargo-less build engine"

[dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }
flux-cache = { path = "../flux-cache" }
